# Local bind-mount: serve tiangong-host registry + artifacts from Docker.
# Usage:
#   docker compose -f docker-compose.yml -f docker-compose.local.yml up -d --build
services:
  distribution:
    volumes:
      - ${TIANGONG_HOST:-/Users/xuwei/code/tiangong-host}:/data/project
