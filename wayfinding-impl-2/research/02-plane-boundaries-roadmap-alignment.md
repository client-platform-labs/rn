# 职责边界 + Roadmap：参考吸收（非权威改写）

- Date: 2026-08-20
- Input: 用户提供的「四大平面职责边界清单 + Phase 0–3 Roadmap」——**仅参考**
- Authority: 蓝图 `blueprint/*` + [01-multi-plane-industrial-remediation.md](./01-multi-plane-industrial-remediation.md)（P1–P17）
- Posture（用户纠正）: **吸收好的处理方案；不全盘照用；不按参考文改写已决架构/合同**

## 1. 姿态

参考文用于启发实施拆分与边界表述，**不是**第二套产品规格。  
冲突时：蓝图字段/状态机/JS 列车默认开/身份谱系 **优先**；参考文让步。

## 2. 建议吸收的做法（纳入实施，不替换蓝图）

| 参考文做法 | 吸收方式 |
|------------|----------|
| 各平面「输入 / 输出 / 禁区」表格式 | 实施票与 CONTEXT 写清**禁区**；内容对齐蓝图边界，不另起平面模型 |
| 契约先行、硬门禁在 CI、软风控在控制面 | 已与蓝图一致 → 保持 |
| E2E 异步、可反向阻断晋级 | 对齐 remediation P7 → 保持 |
| 分列车回滚语义尽早写清 | 对齐 P2 → 地图 A 合同就位 |
| 缺陷→阶段映射表思路 | 用于排期启发；映射到地图 A/B/C/D，**不**引入 Phase 0–3 为正式合同名 |
| 阶梯放量 + 观测自动暂停 | 对齐 P10 → 合同早、实现可分期 |
| 客户端多级兜底 | 对齐 P14 → 吸收 |
| 例外 TTL / 收敛 | 对齐 P17 → 吸收 |

## 3. 明确不照用 / 不改写的部分

| 参考文内容 | 处理 |
|------------|------|
| 状态机 `Testing` / `Partial` 等简化名 | **不采用**；机读仍用蓝图全名 |
| 「中国区自动关闭可执行 OTA」 | **不采用**；JS 列车生产默认开 + 渠道叠加 |
| Phase 0「只定标准不写代码」 | **不照用**；契约以机读 schema/校验落地，可与脚手架并行 |
| 「先 pure-rn，Brownfield 到 Phase 3 才全量」作产品砍刀 | **不照用**为「本图无棕地」；实现依赖序可先绿 pure-rn，Brownfield 仍按已定产品目标进地图 A（深度另钉） |
| Phase 月数 SLA | **不写入**验收合同 |
| 用参考文重写 Destination / 五边界 | **禁止改写** |

## 4. 与票 01 的关系

票 01 锁定的是：**蓝图多平面 + P1–P17 补丁 + 地图 A 切片**。  
参考文只影响「怎么拆实施、怎么写禁区文案」，不升级为权威 Roadmap 文本。
