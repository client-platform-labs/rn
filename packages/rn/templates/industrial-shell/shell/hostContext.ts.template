// #263: ONE definition of this seam lives in @client-platform/shell-core (shared
// by the shell-core OTA client and by generated shells — one protocol, two
// hosts). This file only re-exports it so the generated shell keeps its local
// import graph, the same copy→depend-by-construction pattern as ShellHost.
// Do not re-implement the body here: a local copy is how the generated shell
// and shell-core drift apart.
import { readHostContextFromSidecar } from "@client-platform/shell-core";

export { readHostContextFromSidecar };
