# runtime-sdk (STUB / THROWAWAY)

Contracts only:

- `AppHostKernel` / `RuntimeHost` / `SurfaceHost`
- Capability registry + probe tri-state
- Lifecycle state machine names from issue 06

No native implementation in this prototype.
