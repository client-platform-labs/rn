THROWAWAY fixture — not a real binary

Usage: rn-delivery <command> [options]

Delivery host. Pin version in project contract — NEVER app dependencies.
Optional: `client-platform rn-delivery …` if family umbrella is installed.

Commands:
  build         OS backends via installed adapter plugins
  sign          Enterprise signing roots
  test          Hard gates only (static/Jest/RNTL/contract/codegen)
                E2E = signal, never blocks promote/submit
  release       Promote-same-artifact
  update        JS train (default gate js-standard; policy plugin may elevate)
  submit        Store submit for host trains
  release plan  --dry-run
  update plan   --dry-run (selector + channel_profile)

Plugins (examples):
  adapter-ios | adapter-android | adapter-harmonyos
  channel-profile-cn | release-gate-policy

Hard gates: fingerprint, capability subset, channel_profile, signing, SBOM
Never blocked by: E2E red alone
