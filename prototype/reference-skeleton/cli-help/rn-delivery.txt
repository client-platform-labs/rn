THROWAWAY fixture — not a real binary

Usage: rn-delivery <command> [options]

Delivery host. Version MUST be pinned in project contract (deliveryCli.versionRange).
Do NOT add this package to app dependencies.

Commands:
  build         Orchestrate OS-specific compile backends (does not reimplement Metro)
  sign          Signing orchestration against enterprise-owned roots
  test          Trigger hard gates (static/Jest/RNTL/contract/codegen) — E2E is signal-only
  release       Promote-same-artifact across environments
  update        Publish JS train unit (js-standard | js-gated); respects channel_profile
  submit        Store submit for host trains (needs-native path)
  release plan  --dry-run preview of promote/submit graph
  update plan   --dry-run preview of JS selector + channel gates

Global options:
  --non-interactive
  --json
  --dry-run             Explicit on mutating plan commands (no global dry-run)
  --help

Hard gates (block promote/submit):
  fingerprint match, capability subset, channel_profile, signing, SBOM/attest
Never blocked by: E2E signal failure alone

Trains:
  ios-host | android-host | harmony-host | js | experiment
