# plugin: release-gate-policy (STUB)

Defaults: `js-standard`. Elevates to `js-gated` for pay/login/new sensitive/first IA.
`needs-native` when native/permissions/privacy/SDK/ABI change.
Policy is hot-swappable without core release.
