// base-host synthetic peeled orders entry (#141) — second peeled graph
// referenced by the example peel config. The pack script never reads the
// file; it walks the path list and assigns ids.
export const ORDERS_KIND = "peeled.orders";
export const orders = { kind: ORDERS_KIND };
