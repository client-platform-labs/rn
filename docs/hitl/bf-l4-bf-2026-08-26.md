# M8b HITL — BF L4 steel thread (Branch #22)

**Date:** 2026-08-26  
**Project:** `/Users/xuwei/Work/my-rn-app` (rn init · RN 0.87.0 · brownfield scaffold)  
**Device:** `10CEC62C7R000E3`  
**Node:** 24.19.0

## Prereq applied

```bash
node scripts/apply-brownfield-host-stub.mjs ~/Work/my-rn-app
node scripts/scaffold-bf-rct-host.mjs ~/Work/my-rn-app
rn dev-support remove
rn module init main && rn module link main   # topology B for js-update
```

## Delivery pipe (same as GF M8)

```bash
rn-delivery build --platform android --profile release
rn-delivery release --install                    # adb Success
rn-delivery update --module main --profile release
rn-delivery sign && rn-delivery release && rn-delivery promote
rn-delivery block --reason 'BF L4 rollback drill'
rn-delivery promote                              # re-promote js-update after verify ordering
```

## Candidate metadata

| Train | `artifact_kind` | digest (prefix) |
|-------|-----------------|-----------------|
| app-host release | `app-host` | `7717f98b…` |
| js-update `main` | `js-update` | `c9a5bf563f3a…` (`main-c9a5bf563f3a`) |
| `runtime_fingerprint_digest` | both | `886f9452…` |

## Automated gate

```bash
node scripts/verify-bf-l4-steel-thread.mjs ~/Work/my-rn-app
# → BF L4 verify: PASS (automated gates)
```

Includes: M3b · BF RCT host · `verify-l4-steel-thread` · `verify-js-update-load` (production) · M9 quality gate · block drill.

## Device notes

- Release APK installed via `rn-delivery release --install`
- Brownfield native shell + `RnSurfaceActivity` in same binary (scaffold); RCT NPE fix applied
- `rn doctor --profile brownfield`: L3f release hygiene OK; enterprise topology B satisfied after `module init main`

## Verdict

**M8b / BF L4 — PASS** (automated; same delivery/CP pipe as GF)

*Depth not required for this gate: bundlerUrl wiring, rn-module AAR (#5 remainder).*
