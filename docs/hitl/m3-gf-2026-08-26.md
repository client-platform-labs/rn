# M3 HITL — GF host candidate (Spine #21)

**Date:** 2026-08-26  
**Device:** `10CEC62C7R000E3` (USB, authorized)  
**Project:** `rn init` topology B · RN 0.87.0 · `/tmp/rn-m3-hitl-1787703307`  
**Node:** 24.19.0 (build/install); doctor L0 warns on 26.x only

## Steel-thread commands (executed)

```bash
rn init /tmp/rn-m3-hitl-1787703307 --non-interactive
rn doctor                                    # L3f OK; L0 Node 26 advisory
rn-delivery build --platform android --profile release
rn-delivery validate                         # ok: true
rn-delivery release --install                # promote_to_staging + adb Success
rn-delivery block --reason 'M3 steel-thread rollback drill'
```

## Candidate metadata (sealed)

| Field | Value |
|-------|-------|
| `release_id` | `greenfield-0.87.0-local` |
| `artifact_kind` | `app-host` |
| `platform` | `android` |
| `profile` | `release` |
| `digest` | `df1ad2aa766a80d4a24198838089c59c30f75a64c98f10d804cbbaeaa763df45` |
| `runtime_fingerprint_digest` | `886f945253e7875d0d8b11384c5f50a0922b418ae43280440f23e790a2461e59` |
| APK | `android/app/build/outputs/apk/release/app-release.apk` |

## CP file stub (post-drill)

After `release --install` → staging had 1 entry (`stage: promote`).  
After `block` → staging `[]`, blocked `1` (rollback drill recorded).

## Automated verification

```bash
node scripts/verify-steel-thread.mjs /tmp/rn-m3-hitl-1787703307
node scripts/verify-release-hygiene.mjs /tmp/rn-m3-hitl-1787703307 <apk>
```

## Out of scope (M5–M7)

- Real CP API (#7), signed `js-update`, client `gateBundleLoad` on device

## Verdict

**M3 GF L3 — PASS** (host candidate build + validate + install + block drill)
