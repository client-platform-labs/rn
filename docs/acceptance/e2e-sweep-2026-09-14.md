# E2E sweep — 全链路真机跑测（2026-09-14）

**设备:** 10CEC62C7R000E3 · **耗时:** 124s · **命令:** `bash scripts/e2e/run-all.sh`

## 结果：8 PASS / 3 SKIP / 0 FAIL

| 链路环节 | Chain | 结果 | 说明 |
| --- | --- | --- | --- |
| CLI 工具链 | 01-cli | ✅ PASS | rn/ship 双 CLI |
| 开发 · 多 Metro | 02-debug-multi-bundle | ⊘ SKIP | **Metro 8081/8082 未起**（dev-session 前置，需先 `rn dev`） |
| 构建 · Release 加载 | 03-release-load | ✅ PASS | registry host+JS release、APK 物理可拉可装 |
| 壳全生命周期 | 04-shell-lifecycle | ✅ PASS | init/模块挂载/catalog/调试/部署 |
| **业务包全生命周期**（★核心） | 05-biz-lifecycle | ✅ PASS | init→build→sign→release→promote→OTA→kill→rollback（desk + fixture_second 双包） |
| 壳发布平台（CP） | 06-host-portal | ✅ PASS | 装包台 + 双域名 + 端到端发壳 |
| 离线包管理平台 | 07-biz-portal | ✅ PASS | JS 发版台 + 注册/检索/更新策略 |
| 更新策略 · 灰度 | 08-update-strategy | ✅ PASS | 主动/被动触发、lane 切换 |
| 后台服务 | 09-backend-services | ⊘ SKIP×3 | **data-service 未起**（可选外部 Python 服务，非平台发布链；8001 未监听） |
| iOS 生命周期 | 10-ios-lifecycle | ✅ PASS | xcodebuild simulator、真实 digest |
| **OTA 信任链（真机）** | 11-ota-trust | ⊘ SKIP | **leg B 差分控制未建立**（根因见下） |

## chain-11 leg B 的根因（重要）

leg B 判定"设备在 CRL 正常时下载更新、被篡改时拒载"。本次 **0 artifact download**：

- 当前 4040 的 CP 是容器实例（`projectRoot=/data/project`，storage=file），其 `/v1/crl` 返回 **`seal: null`（未签名）**。
- 设备端按 ADR-024 **fail-closed**：未签名的 CRL → 拒载 → 不下载 → leg B 不可判定（SKIP 而非 FAIL，正确行为）。
- 这是**运行环境状态**，不是代码缺口：chain-11 在今天早些时候的 sweep 中**设备 PASS**（`46c9827`：01/03/05/11 PASS；P1/P2 关闭时 dr-drill 亦验过 `device accepts CRL = true → true`）。

**重验 leg B 的完整设置**（HITL，需重配共享容器，本次未做）：
1. `ship keygen` → 新 RCA + leaf（证书链 ADR-024）
2. 把 RCA 公钥**烘焙进 DUT**（com.rnotaacceptance 的 Kotlin）→ **重建 release APK**
3. 以 cert 模式重启 CP：`RN_DELIVERY_SIGN_KEY_PEM/FILE`（leaf 私钥）+ `RN_DELIVERY_LEAF_CERT` + `RN_DELIVERY_LEAF_PUBKEY_HEX`
4. 用匹配 leaf 签名发布 + promote 一个 js-update 候选
5. `bash scripts/e2e/chain-11-ota-trust.sh`

## 顺带修复（同批）

- `scripts/e2e/run-all.sh`：汇总不再无条件 "全部 chain PASS"——统计 `PASS/SKIP/FAIL` 并在 SKIP>0 时显式 warn（SKIP ≠ PASS）。聚合层不再吞掉 SKIP。

## 结论

每个链路环节都有 E2E 资产且 8/11 真机通过；3 条 SKIP 均为环境前置（Metro / 可选 data-service / CP cert 模式未配），非代码缺口。唯一"该验未验"的 chain-11 leg B 为运行环境状态，重验流程见上。
