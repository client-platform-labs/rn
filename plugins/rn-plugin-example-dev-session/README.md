# Example `dev-session` plugin

Hot-plug proof for map-a/#17: discovered as `kind: "dev-session"`, contributes
Dev Menu items into Dev Support (`contributions.json`).

```bash
pnpm build
pnpm exec rn plugin list   # should list example-dev-session
```
